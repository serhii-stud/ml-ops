## How to launch mlflow on ec2

### Connect via terminal
```
cd /opt/mlflow
docker-compose up -d
```

### Fast fix http error in console
go to chrome://flags/
add IP address with a port to the 
`nsecure origins treated as secure`